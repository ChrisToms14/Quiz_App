{
  "name": "quiz-app",
  "private": true,
  "version": "1.0.0",
  "description": "A sleek vanilla HTML, CSS, and JavaScript quiz application",
  "type": "module",
  "scripts": {
    "dev": "npx http-server -p 8080 -o",
    "build": "echo 'No build needed for vanilla HTML/CSS/JS'",
    "preview": "npx http-server -p 8080"
  }
}
